<div class="row">
  <div class="col-md-3">
    <%= book_cover(@book) %>
  </div>
  <div class="col-md-4">
    <p>Título: <%= @book.title %></p>
    <p>Autor: <%= @book.author.name %></p>
    <p>Categoria: <%= @book.category.name %></p>
  </div>
  <%= book_admin_links(@book) if current_user.admin_role? %>
  <%= book_member_links(@book) if current_user.member_role? %>
</div>
